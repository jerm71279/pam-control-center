# pam-migration-mcp server dependencies
mcp[cli]>=1.0.0
pydantic-settings>=2.0.0
httpx>=0.27.0
uvicorn>=0.30.0

# Azure Key Vault (optional — for production credential loading)
# azure-identity>=1.15.0
# azure-keyvault-secrets>=4.8.0

# Inherited from orchestrator (already in orchestrator requirements)
# requests
# urllib3
