"""ML module for adaptive migration intelligence.

Provides ETL anomaly detection (EWMA + Isolation Forest) and NHI classification
blending (LightGBM + rule-based). All ML is advisory-only.
"""

ML_VERSION = "0.1.0"
